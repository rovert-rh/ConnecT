---
import SolucionImg from "../../assets/Nosotros.png";
---

<div class="relative py-16 px-6 md:px-12 container mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-center">
        <!-- Imagen con fondo dinámico -->
        <div class="relative flex justify-center md:justify-end">
            <!-- Fondo degradado detrás de la imagen -->
            <div
                class="absolute top-4 left-4 right-4 bottom-4 md:left-10 md:right-20 rounded-xl bg-gradient-to-r from-primario to-secundario blur-sm opacity-70"
            >
            </div>

            <!-- Imagen -->
            <img
                src={SolucionImg.src}
                alt="Solución IT"
                class="relative w-full max-w-sm md:max-w-md lg:max-w-lg rounded-xl shadow-xl z-10 transform transition-transform duration-500 hover:scale-105 hover:shadow-2xl"
            />
        </div>

        <!-- Contenido de texto -->
        <div class="bg-white/80 backdrop-blur-sm p-6 rounded-xl shadow-md">
            <h1
                class="text-3xl md:text-4xl lg:text-4xl font-bold text-secundario mb-4"
            >
                Una empresa que utiliza la investigación y crea identidades
                únicas.
            </h1>
            <p class="text-base md:text-lg text-gray-700 mb-4">
                Redinet se fundó hace más de 17 años con el propósito de brindar
                un excelente servicio a nuestros clientes y acompañándolos paso
                a paso en su transformación digital.
            </p>
            <p class="text-base md:text-lg text-gray-700 mb-4">
                Soportamos y aseguramos la continuidad de negocio con
                estrategias de administración al cambio como la universidad
                virtual.
            </p>
        </div>
    </div>
</div>
